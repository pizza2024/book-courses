export type Test = {
    hello: 'world';
};
export type User = {
    id: number;
    username: string;
    email: string;
    birthdate: string;
    is_active: boolean;
};
export type LoginData = {
    username: string;
    password: string;
};
